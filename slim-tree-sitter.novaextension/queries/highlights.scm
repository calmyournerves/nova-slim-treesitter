; HTML
(doctype) @processing.doctype
(tag_name) @tag.name
(attr_name) @tag.attribute.name
(attr_value) @tag.attribute.value
(attr) @identifier.variable
(attr_shortcut_class) @style.selector.identifier.class
(attr_shortcut_id) @style.selector.identifier.id

; Comments
(code_comment) @comment

; Text
(element_text) @string
(verbatim_text) @string

; Ruby
; (ruby_block_control) @comment
; [ "else" ] @keyword.condition

; Embeddings
(embedded_engine_name) @processing

